extends layout

block content
  .container.row.text-center
    .col-md-4.col-md-offset-4.home
      .row
        .col-md-12
          h1 Welcome to Pseudonyms
      .row.buttons
        .col-md-12
          a.btn.btn-default.new(href="#")
            |New Game
          a.btn.btn-default.join(href="#")
            |Join Game 
      .row
        .col-md-12
          include partials/footer
block scripts
    script(src='/js/home.js')
